version: "3.8"

name: cpu-ascender
services:
  core:
    build:
      args:
        - BASE_IMAGE=ubuntu:20.04
        - PYTHON_VERSION=3.8
        - UID=${HOST_UID-1000}
        - GID=${HOST_GID-1000}
      context: ../../
      dockerfile: environments/Dockerfile
    env_file:
      - ../.envs.env
    tty: true
    volumes:
        - ../../:/home/challenger/ascender
    ports:
        - 8000:8000